# 注意：.gitignore 不支援行尾註解。
# `local.properties   # 說明` 會被當成一整個包含空白與 # 的樣式，於是什麼都沒擋到。
# 註解一律自己一行。

# --- 建置產物 ---
build/
*.apk
*.aab
*.ap_
*.dex
*.class

# --- Gradle ---
.gradle/
.kotlin/

# 各台機器的 SDK 路徑不同，不進版控
local.properties

# --- Android Studio / IntelliJ ---
.idea/
*.iml
captures/
.externalNativeBuild/
.cxx/

# --- 簽章金鑰。外洩等於別人可以冒名發版 ---
*.jks
*.keystore
keystore.properties

# 發版資訊小抄。裡面有簽章密碼欄位，絕對不能進版控。
release-info.txt

# --- 本機產出 ---
dist/
*.log
*.hprof

# --- 作業系統 ---
.DS_Store
Thumbs.db

# --- 測試過程的暫存 ---
# ui.ps1 從裝置拉下來的 uiautomator dump
/tools/*.xml
# 驗證畫面時的暫存截圖
/tools/_*.png

# --- 設計稿 ---
# design/*.dc.html 是設計稿原始碼，會進版控。
# 但發佈用的那份是把 2 MB 的編輯器打包進去的產物，每次重新產生都整份變動，
# 不該進 git —— 要改設計是改 .dc.html 再重新產生。
/design/nutrilog-today-redesign.html
